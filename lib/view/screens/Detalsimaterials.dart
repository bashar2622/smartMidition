import 'package:flutter/material.dart';

class DetailsMaterials extends StatelessWidget {
  const DetailsMaterials({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [],
      ),
    );
  }
}
